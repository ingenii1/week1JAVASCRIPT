let weather = "cloudy";
let place = "Manchester";

if (place == "manchester" && weather=="sunny"){
    console.log("Check again!");

} 
else if (place == "Manchester" && weather=="rainy"){
    console.log("better take an umbrella")
} 
else {
    console.log("mmm, it could be either way")
}

